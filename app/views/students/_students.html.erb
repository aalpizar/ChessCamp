<h3><%= status %> Students</h3>

<table class="table">
  <tr>
    <th width="260">Name</th>
    <th width="120">Age</th>
    <th width="150">Date of Birth</th>
    <th width="120">Rating</th>
    <th></th>
    <th></th>
  </tr>

<% students.each do |student| %>
  <tr>
    <td><%= link_to student.proper_name, student_path(student) %></td>
    <td align="center"><%= student.age %></td>
    <td align="center"><%= student.date_of_birth.strftime('%m/%d/%y') %></td>
    <td align="center"><%= student.rating %></td>
    <td><%= link_to 'Edit', edit_student_path(student), class: "button tiny radius" %></td>
  </tr>
<% end %>
</table>
<!-- Add the pagination using will_paginate gem -->
<span class="paginate">
<%= will_paginate students, :previous_label => "Previous&nbsp;", :next_label => "&nbsp;Next" %>
</span>